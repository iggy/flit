import 'package:flutter/material.dart';

/// STUB for ticket P1-14 (plugins list). The implementing agent replaces the
/// body but MUST keep the `PluginsScreen` class name and this file path (the
/// router references it).
class PluginsScreen extends StatelessWidget {
  const PluginsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Plugins')),
      body: const Center(child: Text('Plugins — implemented in ticket P1-14.')),
    );
  }
}
